# Development

```bash
npm install
npm run dev
```

Kod değişikliklerini göndermeden önce:

```bash
npm run check
```

Ana uygulama React/TypeScript, CBS çalışma zamanı `src/gis`, saf yardımcılar `src/lib`, görsel bileşenler `src/components` altında tutulur.
